/* eslint-disable prettier/prettier */
export interface TokenData {
  token0: string;
  token1: string;
  pairAddress: string;
  liquidity: string;
  deployed_address: string;
  pair: 'weth' | 'usdc';
}
